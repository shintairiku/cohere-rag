/**
 * @fileoverview 既存のapi_caller.gsは機能ごとに分割されました。
 * 以下のファイルを参照してください:
 *  - config.gs: 共通設定
 *  - validation.gs: トリガー検証ユーティリティ
 *  - triggers.gs: onOpen/handleSheetEditなどのUIトリガー
 *  - search.gs: 画像検索処理
 *  - vectorization.gs: ベクトル化・UUID管理
 */
